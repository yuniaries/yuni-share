const input=document.querySelector('#map-search'),status=document.querySelector('#search-status');const rows=[...document.querySelectorAll('[data-map-row]')];input.addEventListener('input',()=>{const q=input.value.trim().toLocaleLowerCase();let count=0;for(const row of rows){row.hidden=!row.textContent.toLocaleLowerCase().includes(q);if(!row.hidden)count++;}for(const group of document.querySelectorAll('[data-map-group]'))group.hidden=![...group.querySelectorAll('[data-map-row]')].some(x=>!x.hidden);status.textContent=q?'找到 '+count+' 个入口':'共 '+rows.length+' 个公开入口';document.querySelector('#empty-state').hidden=count!==0;});status.textContent='共 '+rows.length+' 个公开入口';