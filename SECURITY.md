# 加密设计与审查边界

这是一份实现说明，不是“已通过安全审计”的证书。请勿仅凭公开仓库声称生产服务已获得端到端加密认证。

## 密钥与内容

1. 浏览器生成空间主密钥。独立加密密码经 PBKDF2-SHA256（600,000 次、随机 salt）派生 AES-256-GCM 封装密钥，用于保护主密钥。
2. 每次上传随机生成 32 字节文件密钥和 8 字节文件 nonce。文件密钥、nonce、文件名、路径等写入加密元数据，由空间主密钥保护。
3. 分片 IV 为 8 字节文件 nonce + 4 字节大端序分片索引。AAD 绑定协议前缀、上传 ID 和分片索引。分片使用 AES-GCM，认证标签随密文保存。
4. 格式版本 2 先给分片添加模式字节；浏览器支持 gzip 且压缩后更小时，采用无损压缩，再加密。不能宣传为完全没有压缩。解密还原需要执行相应解帧/解压。
5. Passkey 相关路径使用 WebAuthn；空间解锁还涉及 PRF/HKDF 封装，是否可用取决于浏览器和认证器。认证成功不等于自动拥有空间密钥。

## 服务器仍能看到什么

账户邮箱、用户名、密码哈希、会话、文件所属账户、上传/访问相关时间、密文尺寸、部分原始逻辑大小、分片数量、分类提示和是否在文件夹内等信息不是全部加密。头像是独立明文图片接口，不在文件加密范围内。TLS 仍然必要。

服务器保存封装后的密钥材料和密文，不代表服务器端完全没有账户信息。密码较弱时仍可能被离线猜测；源码公开不会消除弱密码风险。

## 必须独立审查的问题

- 同一文件密钥与 nonce 的重用，尤其编辑、重试、断点续传路径；AES-GCM 重用 IV 风险很高。
- AAD 校验、完整性校验、权限隔离、竞态、错误分支与大文件边界。
- 服务端可替换浏览器下载的 JavaScript，进而读取用户输入或密钥；同站 SRI 不能防止同时修改 HTML 的恶意运营者。可复现构建、已签名独立客户端或独立代码分发/审查机制需另行建立。
- 本地注册码不证明邮箱所有权。后端/反向代理限流和运营身份验证仍由部署者负责。
- 依赖安全更新、XSS、终端恶意软件、备份、日志、会话和内存生命周期。

## 本次检查的范围

进行了源码裁剪、明文敏感配置模式扫描、语法检查、针对性自动测试；不等于渗透测试或全面密码学审计。不会对“无法破解”“绝对安全”作保证。报告问题时不要公开真实密钥、恢复链接、数据库或用户分片。
