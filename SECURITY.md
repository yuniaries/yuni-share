# 你的文件与隐私

上传个人文件之前，你可能想知道：文件在哪里加密、谁能读取，以及忘记密码后还能否找回。这份说明帮助你了解本仓库所展示的保护方式及其边界。

## 我的文件如何受到保护？

文件在你的浏览器中加密后才上传。每个文件使用独立的随机密钥，文件内容被拆成分片并通过 AES-GCM 加密。下载时，客户端验证分片的完整性，再解密还原内容。

文件名、路径和文件密钥等信息也会加密保存。保护这些信息的空间主密钥由客户端生成，并由你的独立加密密码保护。服务器保存的是密文和经过加密封装的密钥材料。

这意味着：仅拿到服务器保存的文件分片，并不等于拿到了可直接打开的原文件；还需要相应的解密密钥和元数据。

## 服务器能看到哪些信息？

端到端加密保护文件内容，但不会隐藏所有与账户和存储有关的信息。

- **加密保存的内容**：文件正文、文件名、路径，以及元数据中的文件密钥。
- **服务端仍需处理的信息**：账户邮箱、用户名、登录密码哈希、会话、文件所属账户、相关时间、密文大小、部分原始大小、分片数量及分类提示等。
- **不在文件加密范围内的内容**：账户头像。

因此，加密文件空间不等于匿名服务。访问网站时仍应使用 HTTPS。

## 登录密码和加密密码有什么区别？

登录密码用于验证账户身份；加密密码用于解锁文件空间。两者承担不同职责。

仅重置登录密码，并不能替代文件解密所需的密钥。请为加密密码使用足够长、独立且不易猜测的口令，并妥善保管。弱密码仍可能被猜测，加密算法本身不能弥补这一点。

## 忘记加密密码后，还能打开文件吗？

这取决于你是否还保有其他可用的解锁方式，例如已经配置并兼容当前设备的 Passkey。能够登录账户，并不一定意味着能够解锁文件。

如果所有可用的解锁方式和密钥都已丢失，仅凭服务器中的密文无法直接恢复文件。建议在更换设备或清理密码管理器之前，确认新的设备可以实际解锁，并另外保存重要文件的备份。

## Passkey 在这里有什么作用？

Passkey 可参与账户认证；在支持相关功能且已完成配置的设备上，也可用于保护和解锁空间密钥。

它是否可用取决于浏览器、系统、密码管理器和认证器的支持情况。请不要把“已绑定 Passkey”视为所有设备都能解锁的保证。

## 上传会影响文件内容或画质吗？

代码支持在加密前对分片进行无损 gzip 压缩，仅在浏览器支持且压缩后更小时使用。下载时会执行对应的解压，还原原始内容。

这不是图片或视频的有损压缩，也不涉及降低分辨率或重新转码。

## 我还需要注意什么？

文件加密无法保护已经被恶意软件控制的设备。输入密码、解锁和查看文件时，浏览器必须能够处理明文，因此设备安全、浏览器扩展和访问的网站同样重要。

网页版的代码由网站提供。如果网站被入侵，或提供了被恶意修改的页面，输入的密码或解锁后的内容仍可能面临风险。公开源码让你能够检查这里的实现，但不能单独证明某次访问时网站发送的代码与本仓库完全一致。

建议你：

- 使用可信设备和更新后的浏览器，谨慎安装能够读取网页内容的扩展。
- 核对网站地址，使用 HTTPS，不在陌生页面输入加密密码。
- 不分享密码、恢复链接或可用的密钥材料。
- 为不可替代的文件保留独立备份，不将加密等同于永不丢失。

## 我能从公开代码中确认什么？

你可以查看密钥派生、文件加密、元数据保护和服务端文件权限的具体实现，而不只是阅读功能介绍。

本仓库是核心逻辑的独立审查版本，不是线上完整服务或 Android 客户端。它不提供邮件和支付服务，注册使用管理员注册码；注册码绑定邮箱字符串，但不证明邮箱所有权。相关范围见 [项目介绍](README.md#仓库范围)。

现有测试结果见 [验证记录](VERIFICATION.md)。这些测试并不等同于独立的全面安全审计，仓库也不提供“绝对安全”或“无法破解”的保证。

## 想进一步了解技术细节？

可以在 [客户端代码](public/app.js) 中检索：

- `derivePasswordWrappingKey`、`wrapVaultKey`：PBKDF2-SHA256（600,000 次）与主密钥封装。
- `prepareEncryptedUpload`、`encryptMetadata`：文件密钥生成与元数据保护。
- `chunkIv`、`chunkAad`、`encryptChunk`：分片索引、认证数据与 AES-GCM 加密。
- `decodeFileChunk`：解密后的分片还原。

检查编辑、重试和续传等路径时，密钥与 IV 是否重复使用、完整性验证和账户权限隔离都是重要审查点。

## 如何反馈安全问题？

欢迎反馈你发现的问题。普通代码疑问可以在仓库 Issues 中提出；涉及漏洞利用细节或私人信息时，请先通过 [项目维护者](https://github.com/yuniaries) 确认私下沟通方式。

请勿在公开反馈中附上真实密码、恢复链接、密钥、数据库或用户文件。
